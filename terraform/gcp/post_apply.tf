// post_apply.tf intentionally emptied. Duplicate null_resources and heredoc syntax caused terraform init/plan to fail.
// Guidance and the post-deploy ingress null_resource are consolidated in post_deploy.tf.
// If you need the guidance printed during apply, move a simplified local-exec provisioner into post_deploy.tf or README.md.

diff --git a/.takt/pieces/dev-infra.yaml b/.takt/pieces/dev-infra.yaml
--- a/.takt/pieces/dev-infra.yaml
+++ b/.takt/pieces/dev-infra.yaml
@@ -1,123 +1,152 @@
-1. name: dev-infra
-2. description: Google Cloud (Terraform) IaC 専門開発ピース
-3. max_movements: 50
-4. initial_movement: design
-5. 
-6. personas:
-7.   implementer:
-8.     prompt: |
-9.       あなたは Terraform と Google Cloud の実装スペシャリストです。
-10.       HCLコードの記述、リソースの依存関係の整理、および `gcloud` コマンドによる環境確認が得意です。
-11.   reviewer:
-12.     prompt: |
-13.       あなたは Terraform と Google Cloud アーキテクチャのエキスパートです。
-14.       セキュリティ、保守性、および Google Cloud のベストプラクティスの観点からコードを厳格にレビューします。
-15.   tester:
-16.     prompt: |
-17.       あなたは IaC の品質保証スペシャリストです。
-18.       `terraform plan` の結果検証や、構築後のリソースが設計通り（閉域網設定など）になっているか Bash 等を用いてテストします。
-19.   reporter:
-20.     prompt: |
-21.       あなたは技術ドキュメント作成の専門家です。
-22.       提供された情報から高品質な設計書を作成し、Mermaid図を適切に埋め込みます。
-23. 
-24. policies:
-25.   infra-policy: |
-26.     - Cloud Run: 閉域網（Ingress=internal-and-cloud-load-balancing）を徹底する。
-27.     - Cloud SQL: PostgreSQL 17を使用。Private IPによる接続とし、Public IPは無効化する。
-28.     - Cloud SQL Admin API を有効化し、Cloud SQL Studio を利用可能にする。
-29.     - Load Balancer: HTTPS通信とし、Cloud Armor (WAF) で保護する。
-30.     - プロジェクトIDはアクティブな `gcloud` 設定から取得し、ハードコードしない。
-31. 
-32. knowledge:
-33.   project-layout: project-structure
-34.   customer-reqs: requirements
-35. 
-36. movements:
-37.   - name: design
-38.     persona: implementer
-39.     knowledge: [project-layout, customer-reqs]
-40.     policy: infra-policy
-41.     edit: true
-42.     allowed_tools: [Read, Glob, Grep, Bash, WebSearch, WebFetch, Write]
-43.     instruction: |
-44.       以下のアーキテクチャに基づき、Terraformの構成案を作成してください。
-45.       設計書は Markdown 形式で出力し、Mermaid記法（flowchart, graph, sequenceなど）を用いて図を記述してください。
-46.       図の記述は *.mmd ファイルとして別途保存してください。
-47. 
-48.       1. Load Balancer + WAF (Cloud Armor)
-49.       2. 閉域網内の Cloud Run
-50.       3. 閉域網内の Cloud SQL (PostgreSQL 17)
-51.       4. Cloud SQL Admin API の有効化設定
-52. 
-53.       設計書と .mmd ファイルの出力が完了したら、次のフェーズに進んでください。
-54.     rules:
-55.       - condition: "設計案とMermaid図がMarkdownファイルとして出力された"
-56.         next: report
-57.       - condition: "情報不足で設計できない"
-58.         next: ABORT
-
-59.   - name: report
-60.     persona: reporter
-61.     knowledge: [project-layout, customer-reqs]
-62.     policy: infra-policy
-63.     edit: true
-64.     allowed_tools: [Read, Glob, Bash, Write, Edit]
-65.     instruction: |
-66.       designフェーズで作成された設計書とMermaid図（*.mmdファイル）を受け取りました。
-
-67.       1. `.mmd` ファイルが存在すれば、`@mermaid-js/mermaid-cli` を使用して PNG 画像を生成してください。
-68.          コマンド例: `mmdc -i input.mmd -o output.png`
-69.          もし `mmdc` が見つからない場合は `npm install -g @mermaid-js/mermaid-cli` を実行してインストールしてください。
-70.       2. 生成されたPNG画像をMarkdown形式の設計書に適切に埋め込んでください。
-71.       3. 最終的な設計書ファイルを `design-report.md` として保存してください。
-72.     rules:
-73.       - condition: "Mermaid図がPNG化され、設計書に埋め込まれた最終レポートが生成された"
-74.         next: implement
-75.       - condition: "Mermaid図の生成や埋め込みに失敗した"
-76.         next: ABORT
-
-77.   - name: implement
-78.     persona: implementer
-79.     knowledge: [project-layout, customer-reqs]
-80.     policy: infra-policy
-81.     edit: true
-82.     allowed_tools: [Read, Glob, Grep, Bash, Write, Edit]
-83.     instruction: "設計に基づき、Terraformコード (.tfファイル) を作成・修正してください。"
-84.     rules:
-85.       - condition: "コードの記述が完了した"
-86.         next: review
-
-87.   - name: review
-88.     persona: reviewer
-89.     knowledge: [project-layout, customer-reqs]
-90.     policy: infra-policy
-91.     edit: true
-92.     allowed_tools: [Read, Glob, Grep, Bash, Edit]
-93.     instruction: |
-94.       実装されたコードをレビューしてください。特に以下の点を重点的にチェックします：
-95.       - Cloud Run/SQL が意図せず公開されていないか
-96.       - WAFの設定は適切か
-97.       - PostgreSQLのバージョンは17か
-98.       修正が必要な場合は直接コードを修正するか、具体的な指示を出してください。
-99.     rules:
-100.       - condition: "レビューをパスし、コードが最適化された"
-101.         next: test
-102.       - condition: "大幅な再設計が必要"
-103.         next: implement
-
-104.   - name: test
-105.     persona: tester
-106.     knowledge: [project-layout, customer-reqs]
-107.     policy: infra-policy
-108.     edit: true
-109.     allowed_tools: [Read, Glob, Grep, Bash]
-110.     instruction: |
-111.       `terraform plan` を実行して構成の整合性を確認してください。
-112.       また、セキュリティ設定（隔離環境）がポリシーに準拠しているか静的解析やコマンドで検証してください。
-113.     rules:
-114.       - condition: "すべてのテスト・検証をパスした"
-115.         next: COMPLETE
-116.       - condition: "修正すべき問題が見つかった"
-117.         next: implement
+created
+1. name: dev-infra
+2. description: Google Cloud (Terraform) IaC 専門開発ピース
+3. max_movements: 50
+4. initial_movement: design
+
+6. personas:
+7.   implementer:
+8.     prompt: "Terraform and Google Cloud implementer: author HCL and validate resources with gcloud"
+9.   reviewer:
+10.     prompt: "Terraform reviewer: check security, maintainability, and GCP best practices"
+11.   tester:
+12.     prompt: "IaC QA specialist: run terraform plan and verify closed-network configuration"
+13.   reporter:
+14.     prompt: "Documentation expert: embed Mermaid diagrams into Markdown reports"
+
+16. policies:
+17.   language: "Terraform"
+18.   build: "N/A"
+19.   framework: "Google Cloud Platform / Terraform"
+20.   db: "Cloud SQL (PostgreSQL 17, Private IP)"
+21.   ui_guideline: "n/a"
+22.   layout: "/src-infra for Terraform .tf files"
+23.   infra-policy:
+24.     language: "Terraform"
+25.     build: "N/A"
+26.     framework: "Google Cloud Platform / Terraform"
+27.     db: "Cloud SQL (PostgreSQL 17, Private IP)"
+28.     ui_guideline: "n/a"
+29.     layout: "/src-infra for Terraform .tf files"
+
+31. knowledge:
+32.   project-layout: project-structure
+33.   customer-reqs: requirements
+
+35. movements:
+36.   - name: design
+37.     persona: implementer
+38.     knowledge: [project-layout, customer-reqs]
+39.     policy: infra-policy
+40.     edit: true
+41.     allowed_tools: [Read, Glob, Grep, Bash, WebSearch, WebFetch, Write]
+42.     timeout_minutes: 120
+43.     on_failure: "ABORT"
+44.     instruction: |
+45.       以下のアーキテクチャに基づき、Terraformの構成案を作成してください。
+46.       設計書は Markdown 形式で出力し、Mermaid記法（flowchart, graph, sequenceなど）を用いて図を記述してください。
+47.       図の記述は *.mmd ファイルとして別途保存してください。
+
+48.       1. Load Balancer + WAF (Cloud Armor)
+49.       2. 閉域網内の Cloud Run
+50.       3. 閉域網内の Cloud SQL (PostgreSQL 17)
+51.       4. Cloud SQL Admin API の有効化設定
+
+52.       設計書と .mmd ファイルの出力が完了したら、次のフェーズに進んでください。
+53.     rules:
+54.       - condition: "設計案とMermaid図がMarkdownファイルとして出力された"
+55.         next: report
+56.       - condition: "情報不足で設計できない"
+57.         next: ABORT
+
+59.   - name: report
+60.     persona: reporter
+61.     knowledge: [project-layout, customer-reqs]
+62.     policy: infra-policy
+63.     edit: true
+64.     allowed_tools: [Read, Glob, Bash, Write]
+65.     timeout_minutes: 30
+66.     on_failure: "ABORT"
+67.     install_hint: |
+68.       If `mmdc` is not available, prefer containerized execution in CI or developer machines. Examples:
+69.       - POSIX/CI: `docker run --rm -v "$(pwd)":/data minified/mmdc mmdc -i input.mmd -o output.png`
+70.         Ensure the CI job uses the repository workspace as the container volume (e.g. `$CI_WORKSPACE` mounted to `/data`).
+71.       - Windows (PowerShell): adjust volume mapping, e.g. `docker run --rm -v ${PWD}:/data minified/mmdc mmdc -i input.mmd -o output.png`.
+72.       - Alternatively, install globally via npm: `npm install -g @mermaid-js/mermaid-cli`.
+73.       Note: ensure CI provides necessary environment variables and that Docker is available in the runner.
+74.     instruction: |
+75.       designフェーズで作成された設計書とMermaid図（*.mmdファイル）を受け取りました。
+
+76.       1. `.mmd` ファイルが存在すれば、`@mermaid-js/mermaid-cli` を使用して PNG 画像を生成してください。
+77.          コマンド例: `mmdc -i input.mmd -o output.png`
+78.          もし `mmdc` が見つからない場合は `npm install -g @mermaid-js/mermaid-cli` を実行してインストールしてください。
+79.       2. 生成されたPNG画像をMarkdown形式の設計書に適切に埋め込んでください。
+80.       3. 最終的な設計書ファイルを `design-report.md` として保存してください。
+81.     rules:
+82.       - condition: "Mermaid図がPNG化され、設計書に埋め込まれた最終レポートが生成された"
+83.         next: implement
+84.       - condition: "Mermaid図の生成や埋め込みに失敗した"
+85.         next: ABORT
+
+87.   - name: implement
+88.     persona: implementer
+89.     knowledge: [project-layout, customer-reqs]
+90.     policy: infra-policy
+91.     edit: true
+92.     allowed_tools: [Read, Glob, Grep, Bash, Write]
+93.     timeout_minutes: 240
+94.     on_failure: "ABORT"
+95.     instruction: "設計に基づき、Terraformコード (.tfファイル) を作成・修正してください。"
+96.     rules:
+97.       - condition: "コードの記述が完了した"
+98.         next: review
+
+100.   - name: review
+101.     persona: reviewer
+102.     knowledge: [project-layout, customer-reqs]
+103.     policy: infra-policy
+104.     edit: true
+105.     allowed_tools: [Read, Glob, Grep, Bash, Edit]
+106.     timeout_minutes: 60
+107.     on_failure: "ABORT"
+108.     instruction: |
+109.       実装されたコードをレビューしてください。特に以下の点を重点的にチェックします：
+110.       - Cloud Run/SQL が意図せず公開されていないか
+111.       - WAFの設定は適切か
+112.       - PostgreSQLのバージョンは17か
+113.       修正が必要な場合は直接コードを修正するか、具体的な指示を出してください。
+114.     rules:
+115.       - condition: "レビューをパスし、コードが最適化された"
+116.         next: test
+117.       - condition: "大幅な再設計が必要"
+118.         next: implement
+
+120.   - name: test
+121.     persona: tester
+122.     knowledge: [project-layout, customer-reqs]
+123.     policy: infra-policy
+124.     edit: true
+125.     allowed_tools: [Read, Glob, Grep, Bash]
+126.     timeout_minutes: 30
+127.     on_failure: "ABORT"
+128.     instruction: |
+129.       `terraform plan` を実行して構成の整合性を確認してください。
+130.       また、セキュリティ設定（隔離環境）がポリシーに準拠しているか静的解析やコマンドで検証してください。
+131.     rules:
+132.       - condition: "すべてのテスト・検証をパスした"
+133.         next: COMPLETE
+134.       - condition: "修正すべき問題が見つかった"
+135.         next: implement
+
+# Commit message:
+# takt: dev-infra ポリシー整備と Mermeid CLI 生成の堅牢化
+# Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
